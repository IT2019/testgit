version: "1.0"
tasks:
  - type: run_data_discovery
  - type: mask_table
    table: REPLACE_ME
    # To improve performance, consider specifying multiple wlkjhorkers (workers >1) and increasing batch size.
    # workers: 2
    key: REPLACE_MEdsfsdf
    rules: 
      - column: REPLACE_ME
        masks:
          - type: from_fixed
            value: REPLACE_ME